<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>bifrore</groupId>
        <artifactId>bifrore-parent</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>

    <artifactId>build</artifactId>

    <dependencies>
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
        </dependency>
        <dependency>
            <groupId>ch.qos.logback</groupId>
            <artifactId>logback-classic</artifactId>
        </dependency>
        <dependency>
            <groupId>commons-cli</groupId>
            <artifactId>commons-cli</artifactId>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.dataformat</groupId>
            <artifactId>jackson-dataformat-yaml</artifactId>
        </dependency>
        <dependency>
            <groupId>io.netty</groupId>
            <artifactId>netty-handler</artifactId>
        </dependency>
        <dependency>
            <groupId>io.reactivex.rxjava3</groupId>
            <artifactId>rxjava</artifactId>
        </dependency>
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-core</artifactId>
        </dependency>
        <dependency>
            <groupId>io.vertx</groupId>
            <artifactId>vertx-web</artifactId>
        </dependency>
        <dependency>
            <groupId>io.netty</groupId>
            <artifactId>netty-transport-classes-epoll</artifactId>
        </dependency>
        <dependency>
            <groupId>bifrore</groupId>
            <artifactId>bifrore-rpc</artifactId>
        </dependency>
        <dependency>
            <groupId>bifrore</groupId>
            <artifactId>bifrore-processor-client</artifactId>
        </dependency>
        <dependency>
            <groupId>bifrore</groupId>
            <artifactId>bifrore-router-client</artifactId>
        </dependency>
        <dependency>
            <groupId>bifrore</groupId>
            <artifactId>bifrore-processor-server</artifactId>
        </dependency>
        <dependency>
            <groupId>bifrore</groupId>
            <artifactId>bifrore-router-server</artifactId>
        </dependency>
        <dependency>
            <groupId>bifrore</groupId>
            <artifactId>bifrore-admin</artifactId>
        </dependency>
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-registry-prometheus</artifactId>
        </dependency>
        <dependency>
            <groupId>org.rocksdb</groupId>
            <artifactId>rocksdbjni</artifactId>
        </dependency>
        <dependency>
            <groupId>org.apache.kafka</groupId>
            <artifactId>kafka-clients</artifactId>
        </dependency>
        <dependency>
            <groupId>bifrore</groupId>
            <artifactId>bifrore-common-store</artifactId>
        </dependency>
    </dependencies>
    <build>
        <finalName>bifrore-${project.parent.version}</finalName>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-assembly-plugin</artifactId>
                <executions>
                    <execution>
                        <id>make-assembly</id>
                        <phase>package</phase>
                        <goals>
                            <goal>single</goal>
                        </goals>
                    </execution>
                </executions>
                <configuration>
                    <descriptors>
                        <descriptor>assembly/assembly-standalone.xml</descriptor>
                    </descriptors>
                    <appendAssemblyId>true</appendAssemblyId>
                    <tarLongFileMode>posix</tarLongFileMode>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <profile>
            <id>native</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.graalvm.buildtools</groupId>
                        <artifactId>native-maven-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>build-native</id>
                                <goals>
                                    <goal>compile-no-fork</goal>
                                </goals>
                                <phase>package</phase>
                            </execution>
                        </executions>
                        <configuration>
                            <mainClass>bifrore.starter.StandaloneStarter</mainClass>
                            <imageName>${project.artifactId}</imageName>
                            <verbose>true</verbose>
                            <debug>true</debug>
                            <imageName>bifrore</imageName>
                            <buildArgs>
                                <buildArg>--verbose</buildArg>
                                <buildArg>-H:+PrintClassInitialization</buildArg>
                                <buildArg>-H:+PrintAnalysisCallTree</buildArg>
                                <buildArg>--report-unsupported-elements-at-runtime</buildArg>
                            </buildArgs>
                            <systemProperties>
                                <java.awt.headless>true</java.awt.headless>
                                <io.netty.tryReflectionSetAccessible>true</io.netty.tryReflectionSetAccessible>
                                <io.netty.allocator.cacheTrimIntervalMillis>5000</io.netty.allocator.cacheTrimIntervalMillis>
                                <LOG_DIR>${project.basedir}/logs</LOG_DIR>
                                <CONF_DIR>${project.basedir}/conf</CONF_DIR>
                                <logback.configurationFile>${project.basedir}/conf/logback.xml</logback.configurationFile>
                                <hazelcast.logging.type>slf4j</hazelcast.logging.type>
                                <hazelcast.logo.enabled>false</hazelcast.logo.enabled>
                            </systemProperties>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>